/**
 * Hex equivalent of `--brand-color` (`--sb-cyan-400`).
 * @homecode/ui Theme injects `--active-color` from JS and needs hex for alpha variants.
 */
export const SYBILION_ACTIVE_COLOR = "#00a9c7";
